"use strict";

// index.ts
var Timeforge = class {
  static getCurrentDateTime() {
    return /* @__PURE__ */ new Date();
  }
  static getCurrentUTCDateTime() {
    const currentDate = /* @__PURE__ */ new Date();
    const utcDate = new Date(
      currentDate.getUTCFullYear(),
      currentDate.getUTCMonth(),
      currentDate.getUTCDate(),
      currentDate.getUTCHours(),
      currentDate.getUTCMinutes(),
      currentDate.getUTCSeconds()
    );
    return utcDate;
  }
  static getTimezoneDate(timezoneOffset) {
    const currentDate = /* @__PURE__ */ new Date();
    const offsetInMilliseconds = timezoneOffset * 6e4;
    const timezoneDate = new Date(currentDate.getTime() + offsetInMilliseconds);
    return timezoneDate;
  }
};
console.log(Timeforge.getCurrentDateTime());
console.log(Timeforge.getCurrentUTCDateTime());
module.exports = Timeforge;
